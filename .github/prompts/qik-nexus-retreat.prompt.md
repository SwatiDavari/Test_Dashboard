---
description: 'Move a PI-stage entry\'s progress one step back. Use to correct an incorrect advance.'
---

<!-- @needs nexus retreat procedure (Copilot), prd_unit_imp__nexus__retreat_procedure_copilot, prd_unit_imp, [prd_unit_des__nexus__retreat_procedure_copilot], proposed -->

# qik nexus retreat — retreat PI progress
Use `mcp_qik-mcp_nexus_retreat` or CLI:
```bash
qik nexus retreat <CR_ID>
```

Progress regression: `complete → validate → wip → open`
