# Documentation / traceability toolchain for the `/needs` Sphinx project.
# Produces needs.json (the bridge format consumed by qik axon).
# Install into a dedicated venv via scripts/needs-build.sh.
sphinx>=7.2
sphinx-needs>=4.1
myst-parser>=2.0
pydata-sphinx-theme>=0.20
sphinx-codelinks
sphinxcontrib-mermaid>=1.0
esbonio>=1.0
sphinx-autobuild>=2024.0
